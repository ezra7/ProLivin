<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ezraaigbe7gmail.prolivin">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ExplorePage"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_explore_page" />
        <activity android:name=".Search" />
        <activity android:name=".AthleteProfile" />
        <activity android:name=".AthleteProfile2" />
        <activity android:name=".AthleteProfile3" />
        <activity android:name=".AthleteProfile4" />
        <activity android:name=".AthleteProfile5" />
        <activity android:name=".AthleteProfile6" />
        <activity android:name=".SubscriptionActivity" />
        <activity android:name=".ProLivinScore"></activity>
    </application>

</manifest>